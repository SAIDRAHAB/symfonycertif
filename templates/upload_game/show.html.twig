{% extends 'base.html.twig' %}

{% block title %}UploadGame{% endblock %}

{% block body %}
<div class="container">
    <h1>UploadGame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ upload_game.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ upload_game.Titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ upload_game.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('upload_game_index') }}">back to list</a>

    <a href="{{ path('upload_game_edit', {'id': upload_game.id}) }}">edit</a>

    {{ include('upload_game/_delete_form.html.twig') }}
    </div>
{% endblock %}
