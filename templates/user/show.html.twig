{% extends 'base.html.twig' %}

{% block title %}Utilisateur
{% endblock %}


{% block stylesheets %}
	<link href="{{ asset('css/bg1-alt.css') }}" rel="stylesheet"/>
{% endblock %}


{% block body %}

	<div class="container mb-5 mt-5 ">
		<h1 class="text-center mt-5 mb-5 ">Utilisateur</h1>

		<table class="table table-striped table-primary">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ user.id }}</td>
				</tr>
				<tr>
					<th>Email</th>
					<td>{{ user.email }}</td>
				</tr>
				<tr>
					<th>Pseudo</th>
					<td>{{ user.pseudo }}</td>
				</tr>
				<tr>
					<th>Roles</th>
					<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
				</tr>
				<tr>
					<th>Password</th>
					<td>{{ user.password }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-light" style=" color: #333 !important" href="{{ path('user_index') }}">Revenir à la liste</a>

		<a class="btn btn-warning " href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>

		{{ include('user/_delete_form.html.twig') }}
	{% endblock %}

	{% block footer %}{% endblock %}
